import BalancedBST from "./randomizedBST";
import Treap from "./treaps";

const bst = new Treap();

bst.insert("harsh", Math.random());
bst.insert("kaanu", Math.random());
bst.insert("harish", Math.random());
bst.preview();
